<Window x:Class="ToastFish.NotebookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="学习笔记本"
        Height="620"
        Width="980"
        MinHeight="420"
        MinWidth="760"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="WrappingTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="TextTrimming" Value="None" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Padding" Value="4,3" />
        </Style>
        <Style x:Key="NotebookCellStyle" TargetType="{x:Type DataGridCell}">
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
        </Style>
        <Style x:Key="NotebookRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="MinHeight" Value="34" />
        </Style>
    </Window.Resources>

    <DockPanel Margin="12">
        <TextBlock DockPanel.Dock="Top"
                   Text="学习笔记本"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10" />
        <TabControl>
            <TabItem Header="词汇">
                <DataGrid x:Name="VocabularyGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          EnableRowVirtualization="False"
                          CellStyle="{StaticResource NotebookCellStyle}"
                          RowStyle="{StaticResource NotebookRowStyle}"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="等级" Binding="{Binding jlptLevel}" Width="60" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="词条" Binding="{Binding primaryText}" Width="2*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="释义" Binding="{Binding secondaryText}" Width="3*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="例句/备注" Binding="{Binding detailText}" Width="5*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="添加时间" Binding="{Binding createdAt}" Width="150" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="语法">
                <DataGrid x:Name="GrammarGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          EnableRowVirtualization="False"
                          CellStyle="{StaticResource NotebookCellStyle}"
                          RowStyle="{StaticResource NotebookRowStyle}"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="等级" Binding="{Binding jlptLevel}" Width="60" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="语法" Binding="{Binding primaryText}" Width="2*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="释义" Binding="{Binding secondaryText}" Width="3*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="说明" Binding="{Binding detailText}" Width="5*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="添加时间" Binding="{Binding createdAt}" Width="150" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="例句">
                <DataGrid x:Name="ExampleGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          EnableRowVirtualization="False"
                          CellStyle="{StaticResource NotebookCellStyle}"
                          RowStyle="{StaticResource NotebookRowStyle}"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="等级" Binding="{Binding jlptLevel}" Width="60" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="语法" Binding="{Binding secondaryText}" Width="2*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="提示" Binding="{Binding promptText}" Width="3*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="例句" Binding="{Binding primaryText}" Width="5*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="答案" Binding="{Binding correctAnswer}" Width="2*" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                        <DataGridTextColumn Header="添加时间" Binding="{Binding createdAt}" Width="150" ElementStyle="{StaticResource WrappingTextBlockStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
