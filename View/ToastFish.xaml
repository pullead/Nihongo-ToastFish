<Window x:Class="ToastFish.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToastFish"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource Locator},Path=Main}"
        Title="Nihongo ToastFish" Height="170" Width="400">
    <Grid>
        <TextBox Text="Nihongo ToastFish" Height="20" FontSize="14" BorderThickness="0"  VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,20,0,0" IsReadOnly="True" />
        <TextBox Text="通知式日语轻学习工具" Height="20" FontSize="14" BorderThickness="0" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,40,0,0" IsReadOnly="True"/>
        <TextBox Text="基于开源项目 Uahh/ToastFish 开发" Height="20" FontSize="14" BorderThickness="0" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,60,0,0" IsReadOnly="True"/>
        <TextBox Text="后续将支持 N5-N1 词汇、语法和例句内容库" Height="20" FontSize="14" BorderThickness="0" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,80,0,0" IsReadOnly="True" />
        <TextBox Text="右键托盘图标开始学习或调整设置" BorderThickness="0" Height="20" FontSize="14" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,100,0,0" IsReadOnly="True" />
        <!--<Button Command="{Binding Push}" Content="Push"/>-->
    </Grid>
</Window>
